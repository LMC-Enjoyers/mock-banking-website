
//const endpointRoot = "http://127.0.0.1:5050/";

export default async function addAccount() {
    /*
    const accountForm = document.getElementById('CreateAccount');
    //console.log("it worked");
    accountForm.addEventListener("submit", async function (event) {
      event.preventDefault();
  
      
      // eslint-disable-next-line no-undef
      //const data = new FormData(accountForm);
      const data = new FormData(document.getElementById('CreateAccount') as HTMLFormElement);
      console.log(data)
      console.log("data")
     
      
      
        
      const dataJSON = JSON.stringify(Object.fromEntries(data));
      console.log(dataJSON)

      const response = await fetch(endpointRoot + '/user_create',
        {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: dataJSON
        });
          
        //accountForm.reset();
        
        
        
      
      
  
      
    });
   */
  }

//document.addEventListener("DOMContentLoaded", addAccount);
  

